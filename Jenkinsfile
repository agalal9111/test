pipeline {
  agent { docker { image 'node:latest' } }

        stages {
            stage('checkout SCM'){
              steps{
                   
                   echo "hey"
              }
            }
        }
}
