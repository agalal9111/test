pipeline {
  agent { docker { image 'node:7-alpine' } }

        stages {
            stage('checkout SCM'){
              steps{
                   
                   echo "hey"
              }
            }
        }
}
