pipeline {
    agent { dockerfile true }
        stages {
            stage('checkout SCM'){
              steps{
                   
                   echo "hey"
              }
            }
        }
}
